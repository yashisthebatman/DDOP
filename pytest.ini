[pytest]
markers =
    slow: marks tests as slow to run

# --- MODIFIED: Logging is now disabled for a clean terminal ---
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S