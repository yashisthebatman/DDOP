[pytest]
markers =
    slow: marks tests as slow to run

# ADD THESE LINES FOR LOGGING
log_cli = true
# --- MODIFIED: Set to DEBUG to capture detailed logs ---
log_cli_level = DEBUG
log_cli_format = %(asctime)s [%(levelname)8s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S